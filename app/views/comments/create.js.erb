<% if @comment.errors.any? %>
  sweetAlert("<%= @comment.errors.full_messages.to_sentence %>");
<% else %>
  swal({title: '提交成功'}, function() {
    $("#new_comment textarea").val("");
    $("#comment_box").html("<%= j(render 'comments/comments') %>");
  });
<% end %>
