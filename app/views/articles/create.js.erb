<% if @article.errors.any? %>
  sweetAlert("<%= @article.errors.full_messages.to_sentence %>");
<% else %>
  swal({title: '保存成功！'}, function() {
    window.location.href = "<%= article_path(@article) %>";
  });
<% end %>
